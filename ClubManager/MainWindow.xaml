<Window x:Class="ClubManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#FF2D2D30"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Estilos modernos -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF004578"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF6C757D"/>
                                <Setter Property="Foreground" Value="#FFAAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3F3F46"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF007ACC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#FF3F3F46"/>
            <Setter Property="RowBackground" Value="#FF1E1E1E"/>
            <Setter Property="AlternatingRowBackground" Value="#FF2D2D30"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#FF005A9E"/>
        </Style>

        <Style x:Key="StatusBarStyle" TargetType="StatusBar">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="TitleLabel" TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#FF1E1E1E" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Image Source="{Binding ClubLogo}" Width="40" Height="40" Margin="0,0,15,0" 
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                    <StackPanel VerticalAlignment="Center">
                        <Label Content="{Binding ClubName}" Style="{StaticResource TitleLabel}" Margin="0"/>
                        <TextBlock Text="{Binding CurrentSeason}" Foreground="#FFCCCCCC" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{Binding LicenseStatusColor}" CornerRadius="3" Padding="8,4" Margin="0,0,15,0">
                        <TextBlock Text="{Binding LicenseStatusText}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                    </Border>
                    <TextBlock Text="{Binding CurrentUser}" Foreground="#FFCCCCCC" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="⚙️ Configuración" Style="{StaticResource ModernButton}" 
                            Command="{Binding OpenSettingsCommand}" Margin="0,0,10,0"/>
                    <Button Content="🚪 Salir" Style="{StaticResource ModernButton}" 
                            Command="{Binding ExitCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Menu Lateral -->
        <Border Grid.Row="1" Grid.Column="0" Background="#FF1A1A1A" BorderBrush="#FF3F3F46" BorderThickness="0,0,1,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Label Content="📊 GESTIÓN" Foreground="#FF007ACC" FontWeight="Bold" Margin="20,20,20,10"/>

                    <Button Content="🏠 Dashboard" Style="{StaticResource MenuButton}" 
                            Command="{Binding ShowDashboardCommand}"/>
                    <Button Content="👥 Abonados" Style="{StaticResource MenuButton}" 
                            Command="{Binding ShowAbonadosCommand}"/>
                    <Button Content="🎫 Tipos de Abono" Style="{StaticResource MenuButton}" 
                            Command="{Binding ShowTiposAbonoCommand}"
                            IsEnabled="{Binding CanEdit}"/>

                    <Separator Background="#FF3F3F46" Margin="10,15"/>

                    <Label Content="👥 ORGANIZACIÓN" Foreground="#FF007ACC" FontWeight="Bold" Margin="20,10,20,10"/>

                    <Button Content="🏢 Gestores" Style="{StaticResource MenuButton}" 
                            Command="{Binding ShowGestoresCommand}"
                            IsEnabled="{Binding CanEdit}"/>
                    <Button Content="🎪 Peñas" Style="{StaticResource MenuButton}" 
                            Command="{Binding ShowPeñasCommand}"
                            IsEnabled="{Binding CanEdit}"/>

                    <Separator Background="#FF3F3F46" Margin="10,15"/>

                    <Label Content="🔧 SISTEMA" Foreground="#FF007ACC" FontWeight="Bold" Margin="20,10,20,10"/>

                    <Button Content="👤 Usuarios" Style="{StaticResource MenuButton}" 
                            Command="{Binding ShowUsuariosCommand}"
                            IsEnabled="{Binding CanEdit}"/>
                    <Button Content="📜 Historial" Style="{StaticResource MenuButton}" 
                            Command="{Binding ShowHistorialCommand}"/>
                    <Button Content="🔄 Nueva Temporada" Style="{StaticResource MenuButton}" 
                            Command="{Binding NewSeasonCommand}"
                            IsEnabled="{Binding CanEdit}"/>

                    <Separator Background="#FF3F3F46" Margin="10,15"/>

                    <Label Content="🔐 LICENCIA" Foreground="#FF007ACC" FontWeight="Bold" Margin="20,10,20,10"/>

                    <Button Content="🔑 Cambiar Licencia" Style="{StaticResource MenuButton}" 
                            Command="{Binding ChangeLicenseCommand}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Contenido Principal -->
        <Border Grid.Row="1" Grid.Column="1" Background="#FF2D2D30" Margin="10">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource StatusBarStyle}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding TotalAbonados, StringFormat='Total Abonados: {0}'}" Margin="0,0,20,0"/>
                    <TextBlock Text="{Binding AbonadosActivos, StringFormat='Activos: {0}'}" Margin="0,0,20,0"/>
                    <TextBlock Text="{Binding CurrentDateTime}" Margin="0,0,10,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>