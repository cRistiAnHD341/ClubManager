<Window x:Class="ClubManager.Views.NuevaTemporadaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nueva Temporada" Height="659" Width="734"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF2D2D30">

    <Window.Resources>
        <!-- Estilos modernos -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FF6C757D"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF5A6268"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF495057"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FFDC3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFC82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFA71E2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF007ACC"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style x:Key="ModernLabel" TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="TitleLabel" TargetType="Label">
            <Setter Property="Foreground" Value="#FF007ACC"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="Label">
            <Setter Property="Foreground" Value="#FF007ACC"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>

        <Style x:Key="InfoBorder" TargetType="Border">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="BorderBrush" Value="#FF007ACC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="WarningBorder" TargetType="Border">
            <Setter Property="Background" Value="#FFFF9800"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Label Content="🔄 Nueva Temporada" Style="{StaticResource TitleLabel}"/>
            <TextBlock Text="Configura la nueva temporada deportiva" 
                       HorizontalAlignment="Center" 
                       Foreground="#FFCCCCCC" 
                       FontSize="14" 
                       Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Contenido Principal -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,15,0">
                <!-- Información de la temporada actual -->
                <Border Style="{StaticResource InfoBorder}">
                    <StackPanel>
                        <Label Content="📊 Temporada Actual" Style="{StaticResource SectionTitle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Grid.Column="0" Content="Temporada:" Style="{StaticResource ModernLabel}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TemporadaActual}" 
                                       Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="10,0"/>

                            <Label Grid.Row="1" Grid.Column="0" Content="Total Abonados:" Style="{StaticResource ModernLabel}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TotalAbonados}" 
                                       Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="10,0"/>

                            <Label Grid.Row="2" Grid.Column="0" Content="Abonados Activos:" Style="{StaticResource ModernLabel}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding AbonadosActivos}" 
                                       Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="10,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Configuración de nueva temporada -->
                <Label Content="⚙️ Configuración de Nueva Temporada" Style="{StaticResource SectionTitle}"/>

                <!-- Nombre de la nueva temporada -->
                <Label Content="Nombre de la Nueva Temporada:" Style="{StaticResource ModernLabel}"/>
                <TextBox Style="{StaticResource ModernTextBox}"
                         Text="{Binding NuevaTemporada, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5,0,15"/>

                <!-- Opciones de transición -->
                <Label Content="¿Qué hacer con los abonados actuales?" Style="{StaticResource SectionTitle}"/>

                <RadioButton GroupName="TransicionOptions" 
                             Style="{StaticResource ModernRadioButton}"
                             IsChecked="{Binding MantenerAbonados}"
                             Content="✅ Mantener abonados existentes"/>

                <Border Style="{StaticResource InfoBorder}" Margin="20,5,0,10"
                        Visibility="{Binding MantenerAbonadosInfo}">
                    <StackPanel>
                        <TextBlock Text="Los abonados se mantienen con las siguientes opciones:" 
                                   Foreground="White" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <CheckBox Style="{StaticResource ModernCheckBox}"
                                  IsChecked="{Binding PonerTodosInactivos}"
                                  Content="🔄 Poner todos como inactivos (deberán renovar)"/>

                        <CheckBox Style="{StaticResource ModernCheckBox}"
                                  IsChecked="{Binding RegenerarCodigosBarras}"
                                  Content="🔀 Regenerar códigos de barras"/>

                        <CheckBox Style="{StaticResource ModernCheckBox}"
                                  IsChecked="{Binding MarcarComoNoImpresos}"
                                  Content="🖨️ Marcar todos como no impresos"/>
                    </StackPanel>
                </Border>

                <RadioButton GroupName="TransicionOptions" 
                             Style="{StaticResource ModernRadioButton}"
                             IsChecked="{Binding EmpezarDesdeCero}"
                             Content="🗑️ Empezar desde cero (eliminar todos los abonados)"/>

                <Border Style="{StaticResource WarningBorder}" Margin="20,5,0,15"
                        Visibility="{Binding EmpezarDesdeCeroWarning}">
                    <StackPanel>
                        <TextBlock Text="⚠️ ATENCIÓN: Esta opción eliminará TODOS los abonados" 
                                   Foreground="Black" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock Text="Se mantendrán: gestores, peñas, tipos de abono, usuarios y configuración." 
                                   Foreground="Black" FontSize="12"/>
                        <TextBlock Text="Esta acción NO se puede deshacer." 
                                   Foreground="Black" FontWeight="Bold" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Acciones adicionales -->
                <Label Content="🔧 Acciones Adicionales" Style="{StaticResource SectionTitle}"/>

                <CheckBox Style="{StaticResource ModernCheckBox}"
                          IsChecked="{Binding CrearBackup}"
                          Content="💾 Crear copia de seguridad antes de la transición"/>

                <CheckBox Style="{StaticResource ModernCheckBox}"
                          IsChecked="{Binding ActualizarNumerosSocio}"
                          Content="🔢 Reiniciar numeración de socios desde 1"/>

                <!-- Resumen -->
                <Label Content="📋 Resumen de Cambios" Style="{StaticResource SectionTitle}"/>

                <Border Style="{StaticResource InfoBorder}">
                    <StackPanel>
                        <TextBlock Text="{Binding ResumenCambios}" 
                                   Foreground="White" 
                                   TextWrapping="Wrap"
                                   LineHeight="20"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="❌ Cancelar" 
                    Style="{StaticResource CancelButton}"
                    IsCancel="True"
                    Click="CancelButton_Click" 
                    Margin="0,0,15,0"/>
            <Button Content="💾 Crear Backup" 
                    Style="{StaticResource ModernButton}"
                    Command="{Binding CreateBackupCommand}"
                    Visibility="{Binding ShowBackupButton}"
                    Margin="0,0,15,0"/>
            <Button Content="🔄 Crear Nueva Temporada" 
                    Style="{StaticResource DangerButton}"
                    IsDefault="True"
                    Command="{Binding CreateSeasonCommand}"/>
        </StackPanel>
    </Grid>
</Window>