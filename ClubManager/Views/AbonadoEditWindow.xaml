<Window x:Class="ClubManager.Views.AbonadoEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="828" Width="890"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF2D2D30">

    <Window.Resources>
        <!-- Estilos modernos -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF004578"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF6C757D"/>
                                <Setter Property="Foreground" Value="#FFAAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FF6C757D"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF5A6268"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF495057"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FF28A745"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF218838"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF1E7E34"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF007ACC"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF00D4FF"/>
                            </Trigger>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter Property="BorderBrush" Value="#FFDC3545"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FF007ACC"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="white"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernLabel" TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="RequiredLabel" TargetType="Label" BasedOn="{StaticResource ModernLabel}">
            <Setter Property="Foreground" Value="#FFFF6B6B"/>
        </Style>

        <Style x:Key="TitleLabel" TargetType="Label">
            <Setter Property="Foreground" Value="#FF007ACC"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ErrorTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFDC3545"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Grid.Row="0" Margin="0,0,0,5">
            <Label Content="{Binding WindowTitle}" Style="{StaticResource TitleLabel}"/>
            <TextBlock Text="{Binding SubTitle}" 
                       HorizontalAlignment="Center" 
                       Foreground="#FFCCCCCC" 
                       FontSize="14" 
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Contenido Principal -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="#FF1E1E1E" CornerRadius="10" Padding="30">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Columna Izquierda -->
                    <StackPanel Grid.Column="0">
                        <Label Content="📋 DATOS PERSONALES" 
                               Foreground="#FF007ACC" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

                        <!-- Número de Socio -->
                        <Label Content="* Número de Socio:" Style="{StaticResource RequiredLabel}"/>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding NumeroSocio, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                 Margin="0,5,0,5"/>
                        <TextBlock Text="{Binding NumeroSocioError}" 
                                   Style="{StaticResource ErrorTextBlock}"
                                   Visibility="{Binding HasNumeroSocioError}"/>

                        <!-- Nombre -->
                        <Label Content="* Nombre:" Style="{StaticResource RequiredLabel}"/>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                 Margin="0,5,0,5"/>
                        <TextBlock Text="{Binding NombreError}" 
                                   Style="{StaticResource ErrorTextBlock}"
                                   Visibility="{Binding HasNombreError}"/>

                        <!-- Apellidos -->
                        <Label Content="* Apellidos:" Style="{StaticResource RequiredLabel}"/>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding Apellidos, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                 Margin="0,5,0,5"/>
                        <TextBlock Text="{Binding ApellidosError}" 
                                   Style="{StaticResource ErrorTextBlock}"
                                   Visibility="{Binding HasApellidosError}"/>

                        <!-- DNI -->
                        <Label Content="* DNI:" Style="{StaticResource RequiredLabel}"/>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding DNI, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                 Margin="0,5,0,5"/>
                        <TextBlock Text="{Binding DNIError}" 
                                   Style="{StaticResource ErrorTextBlock}"
                                   Visibility="{Binding HasDNIError}"/>

                        <!-- Código de Barras -->
                        <Label Content="Código de Barras:" Style="{StaticResource ModernLabel}"/>
                        <Grid Margin="0,5,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Style="{StaticResource ModernTextBox}"
                                     Text="{Binding CodigoBarras, UpdateSourceTrigger=PropertyChanged}"
                                     IsReadOnly="True"/>
                            <Button Grid.Column="1" Content="🔄" 
                                    Style="{StaticResource ModernButton}"
                                    Command="{Binding GenerateCodigoBarrasCommand}"
                                    ToolTip="Regenerar código de barras"
                                    Margin="10,0,0,0" Padding="10"/>
                        </Grid>
                    </StackPanel>

                    <!-- Columna Derecha -->
                    <StackPanel Grid.Column="2">
                        <Label Content="⚙️ CONFIGURACIÓN" 
                               Foreground="#FF007ACC" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

                        <!-- Estado -->
                        <Label Content="Estado:" Style="{StaticResource ModernLabel}"/>
                        <CheckBox Style="{StaticResource ModernCheckBox}"
                                  IsChecked="{Binding EsActivo}"
                                  Content="Abonado activo (ha pagado)"
                                  Margin="0,5,0,15"/>

                        <!-- Gestor -->
                        <Label Content="Gestor:" Style="{StaticResource ModernLabel}"/>
                        <ComboBox Style="{StaticResource ModernComboBox}"
                                  ItemsSource="{Binding Gestores}"
                                  SelectedValue="{Binding GestorId}"
                                  DisplayMemberPath="Nombre"
                                  SelectedValuePath="Id"
                                  Margin="0,5,0,15"/>

                        <!-- Peña -->
                        <Label Content="Peña:" Style="{StaticResource ModernLabel}"/>
                        <ComboBox Style="{StaticResource ModernComboBox}"
                                  ItemsSource="{Binding Peñas}"
                                  SelectedValue="{Binding PeñaId}"
                                  DisplayMemberPath="Nombre"
                                  SelectedValuePath="Id"
                                  Margin="0,5,0,15"/>

                        <!-- Tipo de Abono -->
                        <Label Content="* Tipo de Abono:" Style="{StaticResource RequiredLabel}"/>
                        <ComboBox Style="{StaticResource ModernComboBox}"
                                  ItemsSource="{Binding TiposAbono}"
                                  SelectedValue="{Binding TipoAbonoId}"
                                  DisplayMemberPath="Nombre"
                                  SelectedValuePath="Id"
                                  Margin="0,5,0,15"/>
                        <TextBlock Text="{Binding TipoAbonoError}" 
                                   Style="{StaticResource ErrorTextBlock}"
                                   Visibility="{Binding HasTipoAbonoError}"/>

                        <!-- Estado de Impresión -->
                        <Label Content="Estado de Impresión:" Style="{StaticResource ModernLabel}"/>
                        <CheckBox Style="{StaticResource ModernCheckBox}"
                                  IsChecked="{Binding Impreso}"
                                  Content="Marcado como impreso"
                                  Margin="0,5,0,15"/>

                        <!-- Información adicional -->
                        <Border Background="#FF3F3F46" CornerRadius="5" Padding="15" Margin="0,20,0,0">
                            <StackPanel>
                                <TextBlock Text="ℹ️ Información:" 
                                           Foreground="#FF007ACC" 
                                           FontWeight="Bold" 
                                           FontSize="14"/>
                                <TextBlock TextWrapping="Wrap" 
                                           Foreground="#FFCCCCCC" 
                                           FontSize="12" 
                                           Margin="0,10,0,0">
                                    <Run Text="• Los campos marcados con * son obligatorios"/>
                                    <LineBreak/>
                                    <Run Text="• El número de socio debe ser único"/>
                                    <LineBreak/>
                                    <Run Text="• El código de barras se genera automáticamente"/>
                                    <LineBreak/>
                                    <Run Text="• El estado activo indica que el abonado ha pagado"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </ScrollViewer>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
            <Button Content="❌ Cancelar" 
                    Style="{StaticResource CancelButton}"
                    IsCancel="True"
                    Click="CancelButton_Click" 
                    Margin="0,0,15,0"/>
            <Button Content="✅ Guardar" 
                    Style="{StaticResource SuccessButton}"
                    IsDefault="True"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>